# 4-1. マウントとRootfs

時間がないので超簡素版です、すみません

## マウントとは

- 何かしらの**デバイスをフォルダに紐づける**
- 実はrootfs (`/`以下) も、**マウントによって構築**されている
- 特に`/proc` `/dev` `/sys`以下は、ストレージではなく**カーネルに繋がっている**
  - リアルタイムに**OSやデバイスの情報をファイルとして読む**ことができる
